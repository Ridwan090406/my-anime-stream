<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Genre: <%= genre %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen font-sans">

    <%- include('partials/header') %>

    <div class="container mx-auto px-4 py-8">
        <h2 class="text-2xl font-bold mb-6 capitalize flex items-center gap-2">
            <span class="w-2 h-8 bg-blue-500 rounded"></span>
            Genre: <span class="text-blue-400"><%= genre %></span>
        </h2>

        <% if (results && results.length > 0) { %>
            <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-5 gap-4 mb-8">
                <% results.forEach(item => { %>
                    <a href="/detail/<%= item.animeId %>" class="group relative block bg-gray-800 rounded-lg overflow-hidden hover:scale-105 transition duration-300">
                        <img src="<%= item.poster %>" class="w-full h-60 object-cover" alt="Poster">
                        <div class="absolute bottom-0 left-0 w-full bg-black bg-opacity-80 p-2">
                            <h3 class="text-sm font-bold truncate"><%= item.title %></h3>
                            <div class="flex justify-between mt-1 text-xs text-gray-400">
                                <span><%= item.type || 'TV' %></span>
                                <span class="text-yellow-400">⭐ <%= item.score || '?' %></span>
                            </div>
                        </div>
                    </a>
                <% }) %>
            </div>

            <div class="flex justify-center gap-4 mt-10 border-t border-gray-800 pt-6">
                <% if (page > 1) { %>
                    <a href="/genre/<%= genre %>?page=<%= page - 1 %>" class="bg-gray-700 hover:bg-blue-600 px-6 py-2 rounded-full font-bold transition">&larr; Sebelumnya</a>
                <% } %>
                
                <span class="px-4 py-2 text-gray-500 font-mono">Hal. <%= page %></span>

                <% if (results.length >= 10) { %>
                    <a href="/genre/<%= genre %>?page=<%= page + 1 %>" class="bg-blue-600 hover:bg-blue-500 px-6 py-2 rounded-full font-bold transition">Selanjutnya &rarr;</a>
                <% } %>
            </div>

        <% } else { %>
            <p class="text-center text-gray-500 text-xl py-10">Belum ada anime di genre ini.</p>
        <% } %>
    </div>
<%- include('partials/footer') %>
  </body>
</html>

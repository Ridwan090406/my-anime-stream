<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar Isi Anime</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Sembunyikan scrollbar tapi tetap bisa discroll */
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
    </style>
</head>
<body class="bg-gray-900 text-white min-h-screen font-sans">

    <%- include('partials/header') %>

    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold mb-8 text-center text-blue-500">📚 Kamus Anime (A-Z)</h1>

        <% if (library && library.length > 0) { %>
            
            <div class="sticky top-20 z-40 bg-gray-900/95 backdrop-blur py-4 border-b border-gray-700 mb-8">
                <div class="flex gap-2 overflow-x-auto no-scrollbar pb-2">
                    <% library.forEach(group => { %>
                        <a href="#section-<%= group.startWith %>" 
                           class="flex-shrink-0 w-10 h-10 flex items-center justify-center bg-gray-800 hover:bg-blue-600 rounded-lg border border-gray-700 font-bold transition text-sm">
                            <%= group.startWith %>
                        </a>
                    <% }) %>
                </div>
            </div>

            <div class="space-y-12">
                <% library.forEach(group => { %>
                    <div id="section-<%= group.startWith %>" class="scroll-mt-40">
                        
                        <h2 class="text-4xl font-bold text-gray-700 mb-4 border-l-8 border-blue-600 pl-4">
                            <%= group.startWith %>
                        </h2>

                        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-3">
                            <% group.animeList.forEach(anime => { %>
                                <a href="/detail/<%= anime.animeId %>" class="block bg-gray-800 hover:bg-gray-700 p-3 rounded transition flex items-center gap-3">
                                    <span class="w-2 h-2 bg-blue-500 rounded-full"></span>
                                    <span class="text-sm truncate"><%= anime.title %></span>
                                </a>
                            <% }) %>
                        </div>
                    </div>
                <% }) %>
            </div>

        <% } else { %>
            <div class="text-center py-20">
                <p class="text-gray-500">Gagal memuat daftar. Cek endpoint API di app.js.</p>
            </div>
        <% } %>
    </div>
<%- include('partials/footer') %>
</body>
</html>

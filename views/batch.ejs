<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Download Batch - <%= batch ? batch.title : 'Tidak Ditemukan' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen font-sans flex flex-col">

    <%- include('partials/header') %>

    <div class="container mx-auto px-4 py-8 flex-grow">
        
        <% if (batch) { %>
            <div class="bg-gray-800 rounded-xl p-6 shadow-lg mb-8 border border-gray-700">
                <div class="flex flex-col md:flex-row gap-6">
                    <img src="<%= batch.poster %>" class="w-40 rounded-lg shadow-md mx-auto md:mx-0">
                    <div class="flex-1 text-center md:text-left">
                        <h1 class="text-2xl font-bold text-blue-400 mb-2"><%= batch.title %></h1>
                        <p class="text-gray-400 text-sm mb-4">Download paket hemat satu season lengkap.</p>
                        
                        <div class="inline-block bg-gray-900 px-4 py-2 rounded border border-gray-600">
                            <span class="text-green-400 font-bold">Status:</span> <%= batch.status || 'Completed' %>
                        </div>
                    </div>
                </div>
            </div>

            <div class="space-y-6">
                <h2 class="text-xl font-bold border-l-4 border-blue-500 pl-3">📥 Link Download</h2>
                
                <% if (batch.downloadUrl && batch.downloadUrl.length > 0) { %>
                    <% batch.downloadUrl.forEach(res => { %>
                        <div class="bg-gray-800 p-4 rounded-lg border border-gray-700">
                            <h3 class="text-lg font-bold text-yellow-400 mb-3"><%= res.resolution %></h3>
                            
                            <div class="flex flex-wrap gap-2">
                                <% res.links.forEach(server => { %>
                                    <a href="<%= server.url %>" target="_blank" 
                                       class="bg-gray-700 hover:bg-blue-600 text-white px-3 py-2 rounded text-sm transition border border-gray-600 hover:border-blue-400">
                                        <%= server.server %>
                                    </a>
                                <% }) %>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p class="text-center text-red-400">Link download belum tersedia.</p>
                <% } %>
            </div>

        <% } else { %>
            <div class="text-center py-20 bg-gray-800 rounded-xl">
                <div class="text-6xl mb-4">📦❌</div>
                <h2 class="text-2xl font-bold text-gray-300">Batch Belum Tersedia</h2>
                <p class="text-gray-500 mt-2 mb-6">Mungkin anime ini belum tamat atau belum dibuatkan paket batch-nya.</p>
                <a href="/detail/<%= slug %>" class="bg-blue-600 hover:bg-blue-500 text-white px-6 py-2 rounded-lg transition">
                    Kembali ke Episode Eceran
                </a>
            </div>
        <% } %>

    </div>

    <%- include('partials/footer') %>
</body>
</html>

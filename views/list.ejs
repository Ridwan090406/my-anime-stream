<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Samehadaku Stream</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen font-sans">

    <%- include('partials/header') %>

    <div class="container mx-auto px-4 py-8">
        <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
            <span class="text-yellow-400">🔥</span> Update Terbaru (Samehadaku)
        </h2>

        <% if (results && results.length > 0) { %>
            <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-5 gap-4">
                <% results.forEach(item => { %>
                    <a href="/detail/<%= item.endpoint || item.slug || item.animeId || item.linkId %>" class="group relative block bg-gray-800 rounded-lg overflow-hidden hover:scale-105 transition duration-300">
                        
                        <img src="<%= item.poster || item.image || item.thumb %>" class="w-full h-60 object-cover" alt="Poster">
                        
                        <div class="absolute bottom-0 left-0 w-full bg-black bg-opacity-80 p-2">
                            <h3 class="text-sm font-bold truncate"><%= item.title %></h3>
                            <p class="text-xs text-green-400"><%= item.postedBy || item.date || 'Terbaru' %></p>
                        </div>
                    </a>
                <% }) %>
            </div>

            <div class="flex justify-center gap-4 mt-8 border-t border-gray-800 pt-6">
                <% if (page > 1) { %>
                    <a href="/?page=<%= page - 1 %>" class="bg-gray-700 hover:bg-blue-600 px-6 py-2 rounded-full font-bold transition">&larr; Prev</a>
                <% } %>
                <a href="/?page=<%= page + 1 %>" class="bg-blue-600 hover:bg-blue-500 px-6 py-2 rounded-full font-bold transition">Next &rarr;</a>
            </div>

        <% } else { %>
            <div class="text-center py-20 text-gray-500">
                <p>Gagal memuat data Samehadaku.</p>
                <p class="text-xs mt-2">Cek Terminal untuk melihat error.</p>
            </div>
        <% } %>
    </div>
</body>
</html>

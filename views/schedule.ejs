<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jadwal Rilis - MyStream Cyber</title>
</head>
<body class="text-gray-200 min-h-screen flex flex-col font-sans selection:bg-neon-pink selection:text-white">

    <%- include('partials/header') %>

    <div class="container mx-auto px-4 py-8 flex-grow relative z-10">
        <h1 class="text-3xl font-bold mb-8 text-center pb-4 border-b border-white/10">
            <span class="bg-clip-text text-transparent bg-gradient-to-r from-neon-cyan to-neon-purple">
                📅 Jadwal Rilis Mingguan
            </span>
        </h1>

        <% if (schedule && schedule.length > 0) { %> <div class="grid gap-8">
                <% schedule.forEach(dayData => { %> <div class="glass rounded-xl p-6 border border-white/5 hover:border-neon-purple/30 transition duration-500 relative overflow-hidden">
                        
                        <h2 class="text-2xl font-bold text-neon-pink mb-4 uppercase tracking-widest border-b border-white/10 pb-2 flex items-center gap-2">
                            <span class="w-3 h-3 bg-neon-pink rounded-full shadow-[0_0_10px_#ec4899]"></span>
                            <%= dayData.day %> </h2>
                        
                        <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">
                            <% if (dayData.anime_list && dayData.anime_list.length > 0) { %> <% dayData.anime_list.forEach(anime => { %>
                                    <a href="/detail/<%= anime.slug %>" class="group block glass rounded-lg overflow-hidden transition-all duration-300 hover:scale-105 hover:shadow-neon-cyan hover:border-neon-cyan/50 border border-transparent"> <div class="relative pb-[140%] overflow-hidden">
                                            <img src="<%= anime.poster %>" class="absolute top-0 left-0 w-full h-full object-cover group-hover:scale-110 transition duration-700" loading="lazy"> <div class="absolute inset-0 bg-black/60 flex items-center justify-center opacity-0 group-hover:opacity-100 transition duration-300 backdrop-blur-sm"> <span class="bg-neon-cyan text-black rounded-full w-10 h-10 flex items-center justify-center shadow-[0_0_15px_#06b6d4]">▶</span>
                                            </div>
                                        </div>

                                        <div class="p-3">
                                            <h3 class="text-xs font-semibold text-gray-300 group-hover:text-neon-cyan truncate transition"><%= anime.title %></h3> </div>
                                    </a>
                                <% }) %>
                            <% } else { %>
                                <p class="text-gray-500 text-sm col-span-full italic">Tidak ada anime rilis hari ini.</p>
                            <% } %>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="glass p-10 rounded-xl text-center">
                <p class="text-gray-500 text-xl">Jadwal tidak tersedia saat ini.</p>
            </div>
        <% } %>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
